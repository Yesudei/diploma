\begin{tikzpicture}[
  x=1cm,
  y=1cm,
  >=stealth,
  font=\sffamily\small,
  flow/.style={->, draw=blue!50!black, line width=0.85pt},
  activity/.style={
    draw=blue!55!black,
    fill=blue!6,
    rounded corners=8pt,
    minimum width=4.6cm,
    minimum height=0.92cm,
    align=center,
    line width=0.8pt,
    drop shadow={shadow xshift=0.5pt, shadow yshift=-0.5pt, fill=gray!20, opacity=0.4}
  },
  decision/.style={
    diamond,
    draw=orange!65!black,
    fill=orange!8,
    aspect=2.3,
    minimum width=4.0cm,
    minimum height=1.15cm,
    align=center,
    inner xsep=2pt,
    line width=0.8pt,
    drop shadow={shadow xshift=0.5pt, shadow yshift=-0.5pt, fill=gray!20, opacity=0.4}
  },
  terminal/.style={
    circle,
    draw=blue!55!black,
    fill=blue!70!black,
    minimum size=0.52cm,
    inner sep=0pt,
    line width=0.8pt
  },
  flowlabel/.style={
    font=\sffamily\scriptsize\bfseries,
    fill=white,
    inner sep=1.5pt,
    text=gray!30!black
  }
]
  \node[terminal] (start) at (0,0) {};
  \node[activity] (login) at (0,-1.15) {Нэвтрэх / бүртгүүлэх};
  \node[activity] (catalog) at (0,-2.45) {Курсийн каталог үзэх};
  \node[activity] (course) at (0,-3.75) {Курс сонгох};
  \node[decision] (access) at (0,-5.35) {Үзэх эрхтэй\\юу?};
  \node[activity, fill=orange!6] (buy) at (-5.0,-6.65) {Курс худалдан авах};
  \node[activity] (lesson) at (0,-7.85) {Хичээл үзэх};
  \node[activity] (quiz) at (0,-9.20) {Өөрийгөө шалгах};
  \node[decision] (help) at (0,-10.80) {Тусламж\\хэрэгтэй юу?};
  \node[activity, fill=purple!6] (chat) at (5.0,-12.10) {AI туслахаас асуух};
  \node[activity, fill=green!6] (progress) at (0,-13.40) {Ахиц хадгалах};
  \node[terminal] (end) at (0,-14.55) {};

  \draw[flow] (start) -- (login);
  \draw[flow] (login) -- (catalog);
  \draw[flow] (catalog) -- (course);
  \draw[flow] (course) -- (access);
  \draw[flow] (access.west) -- node[flowlabel, above] {Үгүй} (buy.east);
  \draw[flow] (buy.south) |- (lesson.west);
  \draw[flow] (access.south) -- node[flowlabel, right] {Тийм} (lesson.north);
  \draw[flow] (lesson) -- (quiz);
  \draw[flow] (quiz) -- (help);
  \draw[flow] (help.east) -- node[flowlabel, above] {Тийм} (chat.west);
  \draw[flow] (chat.south) |- (progress.east);
  \draw[flow] (help.south) -- node[flowlabel, left] {Үгүй} (progress.north);
  \draw[flow] (progress) -- (end);
\end{tikzpicture}
